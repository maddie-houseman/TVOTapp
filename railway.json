
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE" },
  "deploy": {
    "startCommand": "sh -lc \"cd server && npm run migrate && npm run start\"",
    "healthcheckPath": "/api/health"
  }
}